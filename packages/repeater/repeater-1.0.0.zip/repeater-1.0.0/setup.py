from distutils.core import setup

setup(
        name        = 'repeater',
        version     = '1.0.0',
        py_modules  = ['repeater'],
        author      ='Ads',
        description ='A simple printer for nested lists',
        )
