from distutils.core import setup
setup(
  name = 'naivetcp',
  packages = ['naivetcp'], # this must be the same as the name above
  version = '0.4',
  description = 'A basic library for implementing tcp client and server',
  author = 'rosesonfire',
  author_email = 'ishti_iks@yahoo.com',
  url = 'https://github.com/rosesonfire/naive_tcp', # use the URL to the github repo
  download_url = 'https://github.com/rosesonfire/naive_tcp/tarball/0.2', # I'll explain this in a second
  keywords = ['tcp', 'client', 'server', 'distributed systems', 'networking', 'data link layer', 'socket programming'], # arbitrary keywords
  classifiers = [],
)