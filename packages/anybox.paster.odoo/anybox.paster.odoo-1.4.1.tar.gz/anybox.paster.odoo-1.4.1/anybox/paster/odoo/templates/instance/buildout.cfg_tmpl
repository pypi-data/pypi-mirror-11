[buildout]
parts = pip odoo
versions = versions

[pip]
recipe = collective.recipe.pip
configs = https://raw.githubusercontent.com/anybox/odoo/8.0/requirements.txt
versions = versions

[odoo]
recipe = anybox.recipe.odoo:server
version = ${version_origin}
#addons = local addons_specific
eggs =
    soappy
    PyPDF
    \${pip:eggs}

[versions]
