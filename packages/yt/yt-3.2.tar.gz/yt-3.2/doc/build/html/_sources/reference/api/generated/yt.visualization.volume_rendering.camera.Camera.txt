yt.visualization.volume_rendering.camera.Camera
===============================================

.. currentmodule:: yt.visualization.volume_rendering.camera

.. autoclass:: Camera

   

.. autosummary::
   :toctree:
   
       ~Camera.__init__
       ~Camera.annotate
       ~Camera.draw
       ~Camera.draw_box
       ~Camera.draw_coordinate_vectors
       ~Camera.draw_domain
       ~Camera.draw_grids
       ~Camera.draw_line
       ~Camera.finalize_image
       ~Camera.get_dependencies
       ~Camera.get_information
       ~Camera.get_sampler
       ~Camera.get_sampler_args
       ~Camera.initialize_source
       ~Camera.look_at
       ~Camera.move_to
       ~Camera.new_image
       ~Camera.partition_index_2d
       ~Camera.partition_index_3d
       ~Camera.partition_index_3d_bisection_list
       ~Camera.partition_region_3d
       ~Camera.pitch
       ~Camera.project_to_plane
       ~Camera.roll
       ~Camera.rotate
       ~Camera.rotation
       ~Camera.save_annotated
       ~Camera.save_image
       ~Camera.set_default_light_dir
       ~Camera.set_default_light_rgba
       ~Camera.show
       ~Camera.show_mpl
       ~Camera.show_tf
       ~Camera.snapshot
       ~Camera.switch_view
       ~Camera.update_view_from_matrix
       ~Camera.yaw
       ~Camera.zoom
       ~Camera.zoomin




   

.. autosummary::
   :toctree:
   
       ~Camera.comm
       ~Camera.star_trees


