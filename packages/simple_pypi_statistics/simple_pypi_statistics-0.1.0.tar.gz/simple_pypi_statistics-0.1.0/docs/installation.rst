============
Installation
============

At the command line::

    $ easy_install simple_pypi_statistics

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv simple_pypi_statistics
    $ pip install simple_pypi_statistics
