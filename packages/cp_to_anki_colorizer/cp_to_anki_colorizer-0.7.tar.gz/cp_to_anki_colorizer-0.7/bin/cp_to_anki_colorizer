#!/usr/bin/env python

#import cp_to_anki_colorizer
from cp_to_anki_colorizer import colorize_lines
import sys

print sys.argv

def usage():
	print "Usage: cp_to_anki_colorizer [filename]"

if len(sys.argv) > 2:
	usage()

filename = sys.argv[1]

colorize_lines.colorize_file(filename)
